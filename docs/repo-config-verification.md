# Repository configuration verification

Scratch file for a throwaway PR that proves the repo configuration works end to end:

- the PR labeler maps changed paths to `area/*` labels
- every required status check reports under the exact name `main-protection` requires
- the ruleset blocks merging until checks pass and a code owner approves

This PR is closed without merging; the file goes with it.
